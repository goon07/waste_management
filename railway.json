{
  "build": {
    "builder": "heroku/buildpacks:20",
    "scripts": {
      "postinstall": "composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan route:cache && php artisan config:cache && php artisan view:cache"
    }
  },
  "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
}
